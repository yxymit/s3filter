- create an EC2 instance on AWS (MainServer_demo)
- attach IAM role (EC2_role) to the instance
- WARN: stop instance rather than terminate, unless you know what you are doing

- setup
sudo yum -y update
# sudo yum -y install python-minimal graphviz libgraphviz-dev graphviz-dev pkg-config python-pip awscli
sudo yum -y install graphviz pkg-config python-pip awscli
pip install -r requirements.txt


# install primesieve
# git clone --recursive https://github.com/hickford/primesieve-python
# cd primesieve-python
# pip install . --upgrade

# export PYTHONPATH=/home/ubuntu/s3filter

ulimit -Sn 10000


- Prepare data
- downlaod tool from TPC (https://www.tpc.org/tpc_documents_current_versions/current_specifications5.asp) and generate data
  or TPC for MACOS (https://github.com/gregrahn/tpch-kit/tree/master)
# cd tpch-kit
# rm -r ref_data
# mkdir ref_data
# make MACHINE=MACOS DATABASE=POSTGRESQL
# export DSS_CONFIG=/.../tpch-kit/dbgen
# export DSS_QUERY=$DSS_CONFIG/queries
# export DSS_PATH=/.../tpch-kit/ref_data
# ./dbgen -vf -s 1 -TL -C 10 -S 1
# ./dbgen -vf -s 1 -TL -C 10 -S 2 
# ...

- convert TPC-H dataset to CSV
# python3 tbl2csv.py TODO

- S3
- create S3 bucket (s3filter)